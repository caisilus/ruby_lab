<!DOCTYPE html>
<html>
  <head>
    <title>RubyLab</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="h-full bg-gray-100">
    <header class="md:fixed top-0 w-full z-30" id="header">
      <%= render partial: 'shared/navbar', locals: { nav_name: yield(:nav_name) } %>
    </header>
    <% if content_for? :sidebar %>
      <%= yield :sidebar %>
    <% else %>
      <main class="overflow-y-auto md:mt-16 py-4 px-6">
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
